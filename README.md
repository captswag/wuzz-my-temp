# Wuzz my temp(erature)

A simple script to log temperature, fan speed etc.

## Backlog

1. Take CPU name, GPU name and other hardware name via system args.
2. Take duration as well as a system args.
3. If possible generate a graph as well using gnuplot once the script has finished execution.
4. Handle ctrl+c.
